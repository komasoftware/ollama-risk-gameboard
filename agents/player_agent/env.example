# Risk Player Agent Environment Configuration Example
# ==================================================
# 
# This is an example .env file with placeholder values.
# Copy this file to .env and replace the placeholder values with your actual configuration.
#
# ==================================================

# Google Cloud Configuration
# --------------------------
# Replace with your actual Google Cloud project ID
PROJECT_ID=your-project-id-here

# Replace with your preferred Google Cloud region
REGION=your-region-here

# Replace with your actual Artifact Registry URL
ARTIFACT_REGISTRY=your-region-docker.pkg.dev/your-project-id/your-repository

# Service Configuration
# ---------------------
# Cloud Run service name (can be customized)
SERVICE_NAME=player-agent

# Docker image name (can be customized)
IMAGE_NAME=player-agent

# Docker image tag (use semantic versioning for production)
IMAGE_TAG=latest

# Agent Configuration
# -------------------
# Agent name for the Risk player
AGENT_NAME=RiskPlayer

# MCP Server Configuration
# ------------------------
# Replace with your actual MCP server URL
MCP_SERVER_URL=https://your-mcp-server-url.a.run.app/mcp/stream

# Alternative MCP Server URL (if needed)
MCP_SERVER_ALTERNATIVE_URL=https://your-alternative-mcp-server-url.a.run.app/mcp/stream

# Gemini Model Configuration
# --------------------------
# Gemini model to use for the agent
GEMINI_MODEL=gemini-2.5-flash-lite-preview-06-17

# Google Cloud AI Platform Configuration
# -------------------------------------
# Use Vertex AI for Gemini model
GOOGLE_GENAI_USE_VERTEXAI=TRUE

# Google Cloud location for AI services
GOOGLE_CLOUD_LOCATION=global

# Authentication Configuration
# ----------------------------
# Path to Google Cloud credentials file
GOOGLE_APPLICATION_CREDENTIALS=/secrets.json

# A2A Server Configuration
# ------------------------
# Agent card URL (will be updated to Cloud Run URL after deployment)
AGENT_CARD_URL=http://localhost:8000/

# Optional Configuration
# ----------------------
# Uncomment and configure these if needed

# LOG_LEVEL=INFO
# PORT=8080
# MEMORY=1Gi
# CPU=1
# MAX_INSTANCES=10
# TIMEOUT=300
# MCP_TIMEOUT=30.0
# MCP_SSE_TIMEOUT=30.0

# ================================================== 